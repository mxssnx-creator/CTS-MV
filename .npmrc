# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Force installation to bypass conflicts
force=true

# Strict peer dependencies disabled for compatibility
strict-peer-dependencies=false
