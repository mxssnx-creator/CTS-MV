#!/usr/bin/env node

/**
 * ensure-database.js - Deprecated
 * Redis is now the primary database with automatic initialization
 */

console.log("[v0] Database check: Redis auto-initializes on first use")
process.exit(0)
